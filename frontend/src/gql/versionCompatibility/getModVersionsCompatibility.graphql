query ModVersionsCompatibility($modReference: ModReference!) {
  getModByReference(modReference: $modReference) {
    ...ModKey
    versions {
      id
      version
      game_version
      dependencies {
        mod_id
        condition
      }
    }
  }
}